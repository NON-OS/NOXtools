import * as calldata from "../../calldata/index.js";

export class CalldataSurface {
  readonly staking = calldata.staking;
  readonly namespace = calldata.namespace;
  readonly token = calldata.token;
}
